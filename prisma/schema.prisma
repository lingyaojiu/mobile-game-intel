generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Entry {
  id          Int      @id @default(autoincrement())
  date        String   // YYYY-MM-DD
  category    String
  title       String
  content     String
  summary     String?  // 摘要
  source      String?
  imageUrl    String?  // 缩略图
  link        String?  // 原文链接
  contentHtml String?  // 抓取的文章正文HTML（用于网页内阅读）
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([date])
  @@index([category])
}
