{
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron",
      "schedule": "0 1 * * *"
    }
  ],
  "buildCommand": "cp prisma/schema.postgres.prisma prisma/schema.prisma && npx prisma generate && next build"
}
